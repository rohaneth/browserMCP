export const config = {
    // The endpoint where batched browser events will be sent.
    // This is isolated here so it can be easily configured or overridden in Task 1.7.
    API_URL: "http://localhost:8000/api/v1/events/batch"
};
